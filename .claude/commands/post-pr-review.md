---
description: post-pr-review skill 手順をローカル (plugin install なし) で起動するためのエイリアス。レビュー本文 (body / event / comments[]) を構造化入力として受け取る前提のため、通常は `run-pr-review` skill から Skill ツール経由で呼び出される。手で `/post-pr-review` を直接叩くユースケースは想定していないため argument-hint は持たない。
---

# /post-pr-review (ローカルエイリアス)

このコマンドは `plugins/pr-review/skills/post-pr-review/SKILL.md` へのエイリアスです。
